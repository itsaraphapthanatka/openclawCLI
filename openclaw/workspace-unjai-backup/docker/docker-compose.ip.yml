version: '3.8'

services:
  # HTTP-only deployment for IP address
  line-gateway:
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
  
  analytics:
    ports:
      - "8002:8002"
    environment:
      - HOST=0.0.0.0
      - PORT=8002
